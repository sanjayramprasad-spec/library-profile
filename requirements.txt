# library_profile dependencies.
# One-time install:   pip install -r requirements.txt
#
# Only two libraries, both pure-pip and Windows-friendly (no minimap2/mappy):
#   biopython  - PairwiseAligner (reference alignment) + SeqIO (FASTA/FASTQ)
#   matplotlib - the heatmap and bar figures
# Validated on biopython 1.87; the test suite is the contract, so re-run it if
# you change this pin (the aligned-row str() cast already guards version drift).
biopython>=1.81
matplotlib>=3.5

# Dev only (running the test suite):
#   pip install pytest
