# Tool output directory (written next to the reads file as <reads>_profile/).
*_profile/

# Real, unpublished data must NEVER be committed (references, reads, slides, maps).
Test_data/
*.fastq
*.fq
*.fastq.gz
*.fq.gz
*.pptx
*.dna

# ...but DO ship the small, fully synthetic demo dataset.
!example_data/demo_reads.fastq

__pycache__/
*.pyc
.pytest_cache/
